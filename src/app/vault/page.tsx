import { PhasePlaceholder } from '@/components/phase-placeholder';

export default function VaultPage() {
  return (
    <PhasePlaceholder
      title="Vault"
      subtitle="files · specs · designs"
      phase={6}
      note="Vault file browser. Ships in Phase 6."
    />
  );
}
