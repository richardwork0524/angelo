import { PhasePlaceholder } from '@/components/phase-placeholder';

export default function SessionsPage() {
  return (
    <PhasePlaceholder
      title="Sessions"
      subtitle="recent session history"
      phase={6}
      note="Sessions list + filters. Ships in Phase 6."
    />
  );
}
