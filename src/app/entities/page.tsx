import { PhasePlaceholder } from '@/components/phase-placeholder';

export default function EntitiesPage() {
  return (
    <PhasePlaceholder
      title="Entities"
      subtitle="companies · apps · games · shells · meta"
      phase={4}
      note="Entity grid + etype filters + entity detail with breadcrumb. Ships in Phase 4 of the focused-redesign chain."
    />
  );
}
