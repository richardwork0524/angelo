import { PhasePlaceholder } from '@/components/phase-placeholder';

export default function NotesPage() {
  return (
    <PhasePlaceholder
      title="Notes"
      subtitle="GAP · IDEA · OBS · REVISIT"
      phase={5}
      note="Type tabs + note grid + quick-add modal. Ships in Phase 5."
    />
  );
}
